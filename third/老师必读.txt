因文本比较大，有30M，所以请老师耐心等上几秒，输出哈夫曼树后还有等待几秒的存储，还有几秒的压缩，最后文件结束会有提示的。
压缩前文件有30710KB，压缩后文件是17265KB，解压后文件是30853KB。
虽然解压前和解压后文件大小有一点点的差别，但是内容是完全一样的，老师可以直接验证文件开头和结尾。
老师可以删除掉unzip.txt文件和zip.txt文件，运行exe，或者删掉exe文件，重新用源代码编译运行，结果都是和我这一样的。
老师也可以用自己的txt文件，文件名称改为data.txt就可以了。